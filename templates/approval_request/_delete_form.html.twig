<form method="post" action="{{ path('app_approval_request_delete', {'id': approval_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ approval_request.id) }}">
    <button class="btn">Delete</button>
</form>
